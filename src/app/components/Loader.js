import React from 'react'

import './styles/Loader.css'

const Loader = () => (
    <div class="container-loader">
        <div class="loader">
            <span>Loading...</span>
        </div>
    </div>
)

export default Loader